<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solar.mapper.UserMapper">
    <resultMap id="userMap" type="User">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="gender" column="gender"/>
        <result property="identify" column="identify"/>
        <result property="location" column="location"/>
        <result property="status" column="status"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="registerTime" column="register_time"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="password" column="password"/>
        <result property="headImg" column="head_img"/>
        <result property="alipay" column="alipay"/>
    </resultMap>
    <select id="queryUser" resultMap="userMap">
        SELECT * FROM user WHERE user.password=#{password}
        AND user.phone=#{phoneOrEmail} OR user.email=#{phoneOrEmail}
    </select>
    <select id="queryPassword" resultType="java.lang.String">
        SELECT user.password FROM user WHERE user.identify=#{identify}
        AND user.phone=#{phoneOrEmail} OR user.email=#{phoneOrEmail}
    </select>
    <update id="updateLastLoginTime">
        UPDATE user SET user.last_login_time=CURRENT_TIMESTAMP() WHERE user.id=#{id}
    </update>
    <insert id="addUser" parameterType="User">
        INSERT INTO user(id, name, gender, identify, location, email, phone, password,head_img,alipay)  VALUES (#{id},#{name},#{gender},#{identify},
        #{location},#{email},#{phone},#{password},#{headImg},#{alipay})
    </insert>
</mapper>